DATA SEGMENT
        RESULT DW ?
        CARRY DB 0
        DATA ENDS
CODE SEGMENT
	ASSUME CS:CODE, DS:DATA
START:
	MOV AX,DATA
	MOV DS,AX
	MOV AX,100FH
	MOV BX,0FF23H
	ADD AX,BX
JNC NEXT
        INC CARRY
NEXT:
	MOV RESULT,AX
	MOV AH,4CH
	INT 21H
CODE ENDS
END START
